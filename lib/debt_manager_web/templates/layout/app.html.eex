<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Debt Manager</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </head>
  <body>
    <header class="bg-primary">
      <section class="container">
        <nav role="navigation" class="navbar navbar-expand-lg navbar-dark" >
          <%= link "Debt Manager", to: Routes.page_path(@conn, :index), class: "text-white navbar-brand" %>
          <ul class="navbar-nav  ml-auto">
            <%= if @current_user do %>
              <li class="nav-item">
                <%= link "Sign out", to: Routes.pow_session_path(@conn, :delete), method: :delete, class: "text-light nav-link" %>
              </li>
              <li class="nav-item">
                <%= link "Edit account", to: Routes.pow_registration_path(@conn, :edit), class: "text-light nav-link" %>
              </li>
            <% else %>
              <li class="nav-item">
                <%= link "Sign in", to: Routes.pow_session_path(@conn, :new), class: "text-light nav-link" %>
              </li>
              <li class="nav-item">
                <%= link "Sign up", to: Routes.pow_registration_path(@conn, :new), class: "text-light nav-link" %>
              </li>
            <% end %>
          </ul>
        </nav>
      </section>
    </header>
    <main role="main" class="container">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= @inner_content %>
    </main>
  </body>
</html>
